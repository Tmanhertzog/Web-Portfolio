-----------------
Commands:
-----------------

git commit -am "..."                        (Commits changes locally)

git push origin master                      (Sends commits to github)
git push --set-upstream origin master       (Sets upsteam origin to master)
git push                                    (Sends commits to github from upstream)